{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "alert-error",
  "title": "Alert Error",
  "description": "A destructive alert listing unique error messages.",
  "dependencies": [
    "lucide-react@^0.475.0"
  ],
  "registryDependencies": [
    "@president/alert"
  ],
  "files": [
    {
      "path": "resources/js/components/common/alert-error.tsx",
      "content": "import { AlertCircleIcon } from 'lucide-react';\nimport { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert';\n\nexport default function AlertError({\n    errors,\n    title,\n}: {\n    errors: string[];\n    title?: string;\n}) {\n    return (\n        <Alert variant=\"error\">\n            <AlertCircleIcon />\n            <AlertTitle>{title || 'Something went wrong.'}</AlertTitle>\n            <AlertDescription>\n                <ul className=\"list-inside list-disc text-sm\">\n                    {Array.from(new Set(errors)).map((error) => (\n                        <li key={error}>{error}</li>\n                    ))}\n                </ul>\n            </AlertDescription>\n        </Alert>\n    );\n}\n",
      "type": "registry:component",
      "target": "~/resources/js/components/common/alert-error.tsx"
    }
  ],
  "type": "registry:component"
}